import React from "react";

const Header = (props) => (
    <header className="App-header">
        <ul className="container">
            <li>Looker SSO Embed Demo</li>
        </ul>
    </header>
)

export default Header;